<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://dnakamuraz.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://dnakamuraz.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-26T20:06:56+00:00</updated><id>https://dnakamuraz.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Ancient DNA assembly</title><link href="https://dnakamuraz.github.io/blog/2026/workshop2assembly/" rel="alternate" type="text/html" title="Ancient DNA assembly"/><published>2026-01-18T00:00:00+00:00</published><updated>2026-01-18T00:00:00+00:00</updated><id>https://dnakamuraz.github.io/blog/2026/workshop2assembly</id><content type="html" xml:base="https://dnakamuraz.github.io/blog/2026/workshop2assembly/"><![CDATA[<p>This tutorial is Part 2 of the Museomics Workshop (CVZoo XIV 2025, University of São Paulo, Brazil). All software used in this tutorial were previously installed during Part 1 of the Museomics Workshop: <a href="https://dnakamuraz.github.io/blog/2026/workshop1linux">check it here</a>!</p> <p>Before starting the tutorial, download the resources <a href="https://drive.google.com/drive/folders/1FBC4-8E0TquQqtSvxvlzb8qbxzKSuuGg?usp=sharing">here</a>. If you completed Part 1, the resources were previously downloaded (no need to download them again).</p> <h2 id="what-is-museomics">What is museomics?</h2> <p><strong>Museomics</strong> is the application of genomic and bioinformatic methods to museum specimens, such as skins, bones, teeth, feathers, shells, pinned insects, and herbarium samples. These specimens are typically from extinct or historically collected organisms and were not preserved with DNA analysis in mind. <strong>Paleogenomics</strong> is the study of very old biological material preserved under natural conditions, such as subfossils from caves (e.g., Neanderthals and Denisovans) and permafrost-preserved tissues (e.g., Ice Age megafauna). Archival DNA refers to DNA recovered from any archived biological material, including DNA from museum specimens (historical DNA; hDNA), typically ranging from ~50 to 200 years old, as well as much older biological material (ancient DNA; aDNA), which can range from thousands to millions of years old. However, in the literature, these terms are sometimes used interchangeably. As a shorthand, we will use only the terms “museomics” and “hDNA” throughout this tutorial.</p> <h2 id="challenges-and-solutions">Challenges and solutions</h2> <p>Working with hDNA presents several well-known challenges. DNA molecules are typically <strong>highly fragmented</strong> and present in very low quantities, with <strong>low endogenous content</strong> due to the predominance of environmental and microbial DNA. In addition, <strong>post-mortem damage</strong>—especially cytosine deamination leading to C→T and G→A substitutions—introduces characteristic errors, and <strong>contamination</strong> from modern DNA (human, microbial, or laboratory sources) can overwhelm authentic sequences.</p> <p>The aforementioned challenges have driven the development of both wet-lab and computational solutions. In the laboratory, <strong>dedicated hDNA facilities</strong> with strict contamination control, optimized extraction protocols, and the use of <strong>second-generation sequencing</strong> (NGS) enable the recovery of short DNA fragments (reads). Enzymatic treatments such as <strong>USER</strong> (UDG + Endonuclease VIII) can partially or fully remove deaminated cytosines and abasic sites, reducing post-mortem mutation. <strong>Single-stranded libraries</strong> can minimize the loss of endogenous content. In silico, bioinformatic approaches explicitly model ancient DNA properties, including the use of <strong>damage profiles</strong> to identify and authenticate ancient molecules, <em>in silico</em> contaminant deletion, <strong>iterative mapping</strong> strategies to improve alignment of short and divergent reads, and filtering steps to minimize contamination. Overall, these strategies have provided advances in museomics.</p> <p>Below, you will learn how to preprocess, assemble, and post-process hDNA reads.</p> <h2 id="preprocessing">Preprocessing</h2> <p>There are three FastQ files in <code class="language-plaintext highlighter-rouge">museomics/part2/1_raw_reads</code>, all sequenced from <em>Dendropsophus rhea</em> MZUSP 14458. Initially, we merge these single-end FastQ files that represent the same sample. Assuming you are in <code class="language-plaintext highlighter-rouge">museomics/part2/1_raw_reads</code>, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new directory</span>
<span class="nb">mkdir</span> ../2_merged<span class="p">;</span> <span class="nb">cd</span> ../2_merged

<span class="c"># Merge reads from multiple lanes but same sample</span>
<span class="nb">cat</span> ../1_raw_reads/Drhea<span class="k">*</span> <span class="o">&gt;</span> Drhea_merged.fastq

<span class="c"># Rename headers to avoid redundancy</span>
conda activate seqkit
seqkit rename Drhea_merged.fastq <span class="o">&gt;</span> Drhea_renamed.fastq
conda deactivate
</code></pre></div></div> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: FastQ files are classified into single-end or paired-end reads. Single-end reads are sequenced from only one end per fragment and usually generate a single file per sample (except in cases where multiple lanes of sequencing are performed for the same sample). Paired-end reads are sequenced from both ends (one strand is the read 1 and its complementary strand is the read 2) and usually generate two files per sample (R1 and R2). The choice between these two approaches depends on the research question. If a high-volume of data and lower costs are necessary, single-end reads suffice. Otherwise, identifying structural variants and alternative splicing likely require paired-end reads. </div> <h3 id="quality-assessment">Quality assessment</h3> <p>FastQC is an essential quality control (QC) tool used in bioinformatics to assess the raw data generated by high-throughput sequencers (like Illumina). Developed by Simon Andrews at Babraham Bioinformatics, it provides a quick, visual overview of whether your sequencing data has potential problems, biases, or contamination before you proceed to downstream analysis.</p> <p>Assuming you are in <code class="language-plaintext highlighter-rouge">museomics/part2/2_merged</code>, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate fastqc
fastqc <span class="k">*</span>renamed.fastq
conda deactivate
</code></pre></div></div> <p>The output HTML file <code class="language-plaintext highlighter-rouge">Drhea_renamed_fastqc.html</code> presents plots that can be visualized in the browser.</p> <blockquote> <p><strong>Exercise 1</strong> Check the file <code class="language-plaintext highlighter-rouge">Drhea_renamed_fastqc.html</code>. Describe and create hypotheses to explain the distributions of (1) per base sequence content, (2) sequence length, and (3) sequence duplication levels.</p> <details> <summary>Show answer</summary> (1) The distribution of per base sequence content presents a high proportion of G relative to A, C and T at the ends of reads. A explanation is the high level of PolyG at the ends of reads. PolyG commonly indicate signal decay during the end of sequencing (and not a real G) because G is represented by no color in some platforms.<br/><br/> (2) The sequence length distribution reveals a bicaudal distribution. The first peak around 20-30 bp likely represents fragmented hDNA reads, whereas the the second peak around 98-100 bp likely represents modern DNA contaminants.<br/><br/> (3) The sequence duplication levels reveals that some sequences are duplicated. Possible explanations include PCR duplicates not removed and fragments from different regions of the genome randomly sharing the same sequence. </details> </blockquote> <h3 id="trimming">Trimming</h3> <p>Illumina libraries present adapters that must be trimmed before downstream analyses. Furthermore, hDNA fragments are often very short (sometimes &lt;50 bp). As such, finding unusually long reads (e.g., &gt;200–300 bp in a highly degraded sample) is actually a major red flag. Conversely, while museomics depends on short fragments, there is a limit to how short a read can be while still being useful. Very short fragments (&lt; 20 bp) can introduces massive noise into your data, making it impossible to accurately map reads (assemble hDNA) in next steps.</p> <p>Assuming you are in <code class="language-plaintext highlighter-rouge">museomics/part2/2_merged</code>, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new directory</span>
<span class="nb">mkdir</span> ../3_cutadapt<span class="p">;</span> <span class="nb">cd</span> ../3_cutadapt

<span class="c"># Trim adapters and reads out of size range</span>
conda activate cutadapt
cutadapt <span class="nt">-O</span> 4 <span class="nt">-a</span> AGATCGGAAGAGCACACGTC <span class="nt">-m</span> 21 <span class="nt">-M</span> 90 <span class="nt">-o</span> Drhea_cutadapt.fastq ../2_merged/Drhea_renamed.fastq

<span class="c"># Assess quality of reads</span>
conda activate fastqc
fastqc <span class="k">*</span>fastq
conda deactivate
</code></pre></div></div> <blockquote> <p><strong>Exercise 2</strong> Check the files <code class="language-plaintext highlighter-rouge">Drhea_cutadapt_fastqc.html</code> and <code class="language-plaintext highlighter-rouge">Dtritaeniatus_cutadapt_fastqc.html</code>. Compare both reports with those before trimming. Describe what happened with the per base sequence quality, sequence distribution, overrepresented sequences, and adapter content.</p> <details> <summary>Show answer</summary> The per base sequence quality improved, the sequence distribution is right-skewed with one peak between 20-30 bp, and adapters and other artifacts like PolyG were deleted. </details> </blockquote> <h3 id="deduplicating">Deduplicating</h3> <p>Deleting PCR duplicates is necessary because hDNA libraries originate from a very small number of unique, degraded template molecules that require extensive PCR amplification to reach detectable levels. Consequently, many PCR duplicates are present, which, if not removed, falsely inflate sequencing coverage. Leaving these duplicates in your analysis can lead to significant errors such as false-positive SNP calls, compromising the accuracy of population genetic and phylogenetic inferences.</p> <p>Assuming you are in <code class="language-plaintext highlighter-rouge">museomics/part2/3_cutadapt</code>, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new directory</span>
<span class="nb">mkdir</span> ../4_tally<span class="p">;</span> <span class="nb">cd</span> ../4_tally

<span class="c"># Delete PCR duplicates</span>
conda activate tally
tally <span class="nt">-i</span> ../3_cutadapt/Drhea_cutadapt.fastq <span class="nt">-o</span> Drhea_tally.fastq <span class="nt">--nozip</span> <span class="nt">--with-quality</span>

<span class="c"># Assess quality of reads</span>
conda activate fastqc
fastqc <span class="k">*</span>fastq
conda deactivate
</code></pre></div></div> <blockquote> <p><strong>Exercise 3</strong> Check the files <code class="language-plaintext highlighter-rouge">Drhea_tally_fastqc.html</code> and <code class="language-plaintext highlighter-rouge">Dtritaeniatus_tally_fastqc.html</code>. Compare both reports with those before deduplicating. Describe what happened with the total number of sequences (available in Basic Statistics) and the sequence duplication levels.</p> <details> <summary>Show answer</summary> The total number of sequences slighly reduced because duplicates were deleted. </details> </blockquote> <h3 id="decontaminating">Decontaminating</h3> <p>In museomics, deleting contaminants using FastQ Screen is a critical step for validating the authenticity of historical DNA and preventing the analysis of modern biological noise. Because DNA from museum specimens is often highly degraded, non-target DNA—originating from the museum environment, laboratory handling (human DNA), or even the reagents themselves—can easily outnumber the endogenous DNA of interest. FastQ Screen allows researchers to map their sequencing reads against a panel of diverse reference genomes (e.g., human, common bacteria, or potential contaminants like PhiX) to quantify and then filter out sequences that do not belong to the target species.</p> <p>Assuming you are in <code class="language-plaintext highlighter-rouge">museomics/part2/4_tally</code>, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new directory</span>
<span class="nb">mkdir</span> ../5_fastqscreen<span class="p">;</span> <span class="nb">cd</span> ../5_fastqscreen
</code></pre></div></div> <p>FastqScreen is a script to map reads against a database of potential contaminant genomes and delete the mapped reads. The first step is creating the database of contaminants. It is located in <code class="language-plaintext highlighter-rouge">museomics/part2/FastqScreenGenomes.zip</code>. Unzip this directory and check the subdirectories containing indexed contaminant genomes. The file <code class="language-plaintext highlighter-rouge">fastq_screen.conf</code> should be edited in each analysis to specify the path for Bowtie2 aligner (L12; check it using the command <code class="language-plaintext highlighter-rouge">which bowtie2</code>), number of threads (cores) for parallel analyses (line 21), and contaminant databases (<em>E. coli</em>, vector, adapters, and <em>Paraburkholderia</em>).</p> <p>In this tutorial, indexing genomes or editing the configuration file are not necessary for FastqScreen analyses (unless the paths in the configuration file are not working). Moreover, human genome was ignored as a potential contaminant to accelerate analyses.</p> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: Indexing refers to two different processes in molecular biology. <br/> First, in library preparation (wet lab), indexing is the process of adding a unique "ID tag" to your DNA fragments in multiplex libraries (i.e. containing multiple samples to be sequenced together). Without these tags, you would have millions of sequences but no way to know which read came from which specimen. After sequencing, the sample of each read is identified during a computational step called demultiplexing. <br/> Second, in read mapping (bioinformatics), you need to "index" your reference genome. This is very similar to the index at the back of a textbook. One possibility is creating a separate set of files (e.g. hash table) that split the reference genome into k-mers and inform their correspondent positions in the genome. Another tool for indexing is the Burrows-Wheeler Transform (BWT), in which it reorders the genome millions of times (so that similar characters are grouped together) and keep the last column. If you tried to align a read by searching the 3-billion-base human genome from start to finish for every single read, it would take years. With an index, the software can "jump" directly to the potential matching locations in milliseconds. </div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate fastqscreen

<span class="c"># Define the configuration file (don't copy and paste the command below, check the path in your computer)</span>
<span class="nv">config</span><span class="o">=</span><span class="s2">"/mnt/c/Users/Aluno/Downloads/CVZoo-20260120T180729Z-3-001/CVZoo/FastqScreenGenomes/fastq_screen.conf"</span>

<span class="c"># New contaminants might be indexed with the following command: bowtie2-build contaminant_name.fasta name_index</span>
fastq_screen <span class="nt">--nohits</span> <span class="nt">--aligner</span> bowtie2 <span class="nt">--conf</span> <span class="nv">$config</span> ../4_tally/Drhea_tally.fastq

<span class="c"># If it fails, edit the path of each database and the commands above again</span>
nano <span class="nv">$config</span>

<span class="c"># Assess quality of reads</span>
conda activate fastqc
fastqc <span class="k">*</span>fastq
conda deactivate
</code></pre></div></div> <blockquote> <p><strong>Exercise 4</strong> Open the files <code class="language-plaintext highlighter-rouge">Drhea_tally_screen.html</code> and <code class="language-plaintext highlighter-rouge">Dtritaeniatus_tally_screen.html</code> in your browser. Which contaminant presents more hits?</p> <details> <summary>Show answer</summary> Paraburkholderia. </details> </blockquote> <h2 id="assembly">Assembly</h2> <p>Sanger sequencing generates few contigs (up to 900 bp) based on chromatogram peaks. In contrast, next-generation sequencing (NGS) handle millions of short reads (usually 50–150 bp) with low error rates (in second-generation) or a few long reads (10k-1M bp) with high error rates (in third-generation). NGS read mapping (alignment) acts like a “shortcut” by using an existing reference genome as a blueprint to organize these tiny fragments. NGS de novo assembly is the most complex approach, building a entirely new genome from scratch by finding overlaps between reads without any external guide—a task that is often impossible for hDNA due to the lack of sufficient overlap between highly fragmented, short reads.</p> <p>As such, in museomics, we use second-generation sequencing and short read mapping.</p> <h3 id="indexing-and-linear-mapping">Indexing and linear mapping</h3> <p>You will run BWA using two samples (<em>D. rhea</em> and <em>D. tritaeniatus</em>) and three references (the mitochondrial 12S and the nuclear 28S and RAG1 of <em>D. microcephalus</em>).</p> <p>Assuming you are in the directory <code class="language-plaintext highlighter-rouge">museomics/part2/5_fastqscreen</code>, let’s index the reference sequences.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new directory</span>
<span class="nb">mkdir</span> ../6_bwa<span class="p">;</span> <span class="nb">cd</span> ../6_bwa
conda activate bwa
<span class="nb">mkdir </span>bwa_index<span class="p">;</span> <span class="nb">cd </span>bwa_index

<span class="c"># Index reference of 28S</span>
<span class="nb">mkdir </span>Dmicrocephalus_28s<span class="p">;</span> <span class="nb">cd </span>Dmicrocephalus_28s
bwa index <span class="nt">-p</span> Dmicrocephalus_28s ../../../reference/Dmicrocephalus_28s.fas

<span class="c"># Index reference of 12S</span>
<span class="nb">mkdir</span> ../Dmicrocephalus_12s<span class="p">;</span> <span class="nb">cd</span> ../Dmicrocephalus_12s
bwa index <span class="nt">-p</span> Dmicrocephalus_12s ../../../reference/Dmicrocephalus_12s.fas

<span class="c"># Index reference of RAG1</span>
<span class="nb">mkdir</span> ../Dmicrocephalus_rag1<span class="p">;</span> <span class="nb">cd</span> ../Dmicrocephalus_rag1
bwa index <span class="nt">-p</span> Dmicrocephalus_rag1 ../../../reference/Dmicrocephalus_rag1.fas

<span class="nb">cd</span> ../..
</code></pre></div></div> <p>Now, we can map reads to the indexed references. We can also convert the output SAI files to SAM files. Assuming you are in the directory <code class="language-plaintext highlighter-rouge">museomics/part2/6_bwa</code>, run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 28S</span>
<span class="nb">mkdir </span>28s<span class="p">;</span> <span class="nb">cd </span>28s
<span class="c"># Align reads</span>
bwa aln <span class="nt">-l</span> 1024 <span class="nt">-n</span> 0.01 <span class="nt">-t</span> 20 ../bwa_index/Dmicrocephalus_28s/Dmicrocephalus_28s ../../5_fastqscreen/Drhea_tally.tagged_filter.fastq <span class="o">&gt;</span> Drhea_28s.sai
<span class="c"># Convert .sai to .sam (bwa samse -f sam index sai fastq)</span>
bwa samse <span class="nt">-f</span> Drhea_28s.sam ../bwa_index/Dmicrocephalus_28s/Dmicrocephalus_28s Drhea_28s.sai ../../5_fastqscreen/Drhea_tally.tagged_filter.fastq

<span class="c"># 12S</span>
<span class="nb">mkdir</span> ../12s<span class="p">;</span> <span class="nb">cd</span> ../12s
<span class="c"># Align reads</span>
bwa aln <span class="nt">-l</span> 1024 <span class="nt">-n</span> 0.01 <span class="nt">-t</span> 20 ../bwa_index/Dmicrocephalus_12s/Dmicrocephalus_12s ../../5_fastqscreen/Drhea_tally.tagged_filter.fastq <span class="o">&gt;</span> Drhea_12s.sai
<span class="c"># Convert .sai to .sam (bwa samse -f sam index sai fastq)</span>
bwa samse <span class="nt">-f</span> Drhea_12s.sam ../bwa_index/Dmicrocephalus_12s/Dmicrocephalus_12s Drhea_12s.sai ../../5_fastqscreen/Drhea_tally.tagged_filter.fastq

<span class="c"># RAG1</span>
<span class="nb">mkdir</span> ../rag1<span class="p">;</span> <span class="nb">cd</span> ../rag1
<span class="c"># Align reads</span>
bwa aln <span class="nt">-l</span> 1024 <span class="nt">-n</span> 0.01 <span class="nt">-t</span> 20 ../bwa_index/Dmicrocephalus_rag1/Dmicrocephalus_rag1 ../../5_fastqscreen/Drhea_tally.tagged_filter.fastq <span class="o">&gt;</span> Drhea_rag1.sai
<span class="c"># Convert .sai to .sam (bwa samse -f sam index sai fastq)</span>
bwa samse <span class="nt">-f</span> Drhea_rag1.sam ../bwa_index/Dmicrocephalus_rag1/Dmicrocephalus_rag1 Drhea_rag1.sai ../../5_fastqscreen/Drhea_tally.tagged_filter.fastq

<span class="nb">cd</span> ..
</code></pre></div></div> <p>Finally, assuming you are in the directory <code class="language-plaintext highlighter-rouge">museomics/part2/6_bwa</code>, we will compress SAM files to BAM and remove unmapped reads.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate samtools

<span class="nb">cd </span>28s
<span class="c"># Convert .sam to .bam</span>
samtools view <span class="nt">--threads</span> 4 <span class="nt">-S</span> <span class="nt">-bh</span> Drhea_28s.sam <span class="o">&gt;</span> Drhea_28s_mapANDunmap.bam
<span class="c"># Remove unmapped reads</span>
samtools view <span class="nt">--threads</span> 4 <span class="nt">-bh</span> <span class="nt">-F</span> 4 Drhea_28s_mapANDunmap.bam <span class="o">&gt;</span> Drhea_28s_map.bam

<span class="nb">cd</span> ../12s
<span class="c"># Convert .sam to .bam</span>
samtools view <span class="nt">--threads</span> 4 <span class="nt">-S</span> <span class="nt">-bh</span> Drhea_12s.sam <span class="o">&gt;</span> Drhea_12s_mapANDunmap.bam
<span class="c"># Remove unmapped reads</span>
samtools view <span class="nt">--threads</span> 4 <span class="nt">-bh</span> <span class="nt">-F</span> 4 Drhea_12s_mapANDunmap.bam <span class="o">&gt;</span> Drhea_12s_map.bam

<span class="nb">cd</span> ../rag1
<span class="c"># Convert .sam to .bam</span>
samtools view <span class="nt">--threads</span> 4 <span class="nt">-S</span> <span class="nt">-bh</span> Drhea_rag1.sam <span class="o">&gt;</span> Drhea_rag1_mapANDunmap.bam
<span class="c"># Remove unmapped reads</span>
samtools view <span class="nt">--threads</span> 4 <span class="nt">-bh</span> <span class="nt">-F</span> 4 Drhea_rag1_mapANDunmap.bam <span class="o">&gt;</span> Drhea_rag1_map.bam
</code></pre></div></div> <p><img src="/assets/img/museomics_part2_1.png" alt="BWA mapping of 12S"/></p> <blockquote> <p><strong>Exercise 5</strong> The command <code class="language-plaintext highlighter-rouge">samtools stats FILE.bam &gt; FILE.txt</code> provides basic statics about a BAM file (read alignment). Compare the BAM files generated for 12S, 28S, and RAG1. Create a hypothesis to explain the differences in number of mapped reads between nuclear and mitochondrial genes.</p> <details> <summary>Show answer</summary> In terms of the number of mapped reads, the observed pattern was 12S &gt; 28S &gt; RAG1. This can be explained primarily by differences in gene copy numbers rather than reference length. Although 28S is physically longer than 12S, the latter exhibited much higher sequencing depth. This is due to the presence of multiple mitochondria per cell, which significantly increases the likelihood of capturing mitochondrial DNA (mtDNA) fragments. Similarly, 28S yielded more reads than the nuclear RAG1 gene because the 28S rRNA gene exists in multiple tandem copies within the nucleolus, whereas RAG1 is typically present as a single-copy gene. </details> </blockquote> <h3 id="reference-bias">Reference bias</h3> <p>Reference bias is an error where the mapped reads are reference-dependent. In museomics, this is particularly dangerous because hDNA is very short. If a read has too many differences from the reference, the aligner decides it’s “too messy” and throws it away or assigns it a low Mapping Quality (MapQ). As such, the final results will look “more like the reference” than the actual sequenced organism.</p> <p>Although out of the scope of this short workshop, possible solutions for reference bias include:</p> <ul> <li>(1) running baiting and iterative mapping (e.g. MITObim), so that reference bias can emerge in the first iteration, but new references are created iteratively based on the contigs of previous iteration.</li> <li>(2) running multiple linear mappings against different references, aligning output consensus sequences, and masking ambiguities with IUPAC N;</li> <li>(3) running a multiple sequence alignment (MSA) of potential references, calling the consensus of the MSA and coding SNPs with IUPAC ambiguities, and mapping reads against this synthetic reference (suggested in <a href="https://academic.oup.com/bib/article/22/5/bbab076/6217726">Oliva et al. 2021</a>);</li> <li>(4) running multiple linear mappings against different references, selecting best mappings using mapping quality indexes, merging mappings via lift over procedure (developed by <a href="https://link.springer.com/article/10.1186/s13059-020-02229-3">Chen et al. 2021</a>)</li> <li>(5) building variation graph from different references and mapping reads against the graph (see tutorials <a href="https://pangenome-hackathon-genotoul-bioinfo-11d6d4f47ac33734abfa2a1377.pages-forge.inrae.fr/pages/tutorial_pangenome_graph/">here</a> and <a href="https://gtpb.github.io/CPANG22/">here</a>).</li> </ul> <h2 id="post-processing">Post-processing</h2> <h3 id="consensus-calling">Consensus calling</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a new directory</span>
<span class="nb">mkdir</span> ../7_consensus<span class="p">;</span> <span class="nb">cd</span> ../7_consensus
conda activate samtools

<span class="c"># BWA: 12S</span>
samtools <span class="nb">sort</span> ../6_bwa/12s/Drhea_12s_map.bam | samtools consensus <span class="nt">-f</span> FASTA - <span class="nt">-o</span> Drhea_12s_consensus.fas

<span class="c"># BWA: 28S</span>
samtools <span class="nb">sort</span> ../6_bwa/28s/Drhea_28s_map.bam | samtools consensus <span class="nt">-f</span> FASTA - <span class="nt">-o</span> Drhea_28s_consensus.fas

<span class="c"># BWA: RAG1</span>
samtools <span class="nb">sort</span> ../6_bwa/rag1/Drhea_rag1_map.bam | samtools consensus <span class="nt">-f</span> FASTA - <span class="nt">-o</span> Drhea_rag1_consensus.fas
</code></pre></div></div> <blockquote> <p><strong>Exercise 6</strong> There are strings of contiguous IUPAC N in the consensus sequences. Is IUPAC N the same as missing data? Why is IUPAC N common in museomics?</p> <details> <summary>Show answer</summary> IUPAC N is common in museomics due to low coverage.IUPAC N and question marks ? (missing data) are not the same. IUPAC N represents any nucleotide, whereas a question mark represents any nucleotide or a gap. The user can specify whether low coverage will be coded as N or ?, but the common approach is coding it with N. However, coding it with N is less coservative (more assumptions) than coding it with question marks. </details> </blockquote> <h3 id="blast">Blast</h3> <p>BLAST (Basic Local Alignment Search Tool) is one of the most fundamental algorithms in bioinformatics. It is used to compare a “query” sequence (DNA, RNA, or protein) against a massive database of known sequences to find regions of similarity. That is, Blast is a quick method to identify sequences.</p> <ol> <li>Visit https://blast.ncbi.nlm.nih.gov/Blast.cgi?PROGRAM=blastn&amp;PAGE_TYPE=BlastSearch&amp;LINK_LOC=blasthome</li> <li>Copy the consensus sequences generated in this tutorial in “Enter query sequence” and clicl on BLAST.</li> <li>Check the identity of each sequence</li> </ol>]]></content><author><name>Daniel Y. M. Nakamura</name></author><category term="workshop"/><category term="museomics"/><category term="tutorial"/><summary type="html"><![CDATA[Part 2 of the Museomics Workshop]]></summary></entry><entry><title type="html">Introduction to Linux for bioinformatics</title><link href="https://dnakamuraz.github.io/blog/2026/workshop1linux/" rel="alternate" type="text/html" title="Introduction to Linux for bioinformatics"/><published>2026-01-06T00:00:00+00:00</published><updated>2026-01-06T00:00:00+00:00</updated><id>https://dnakamuraz.github.io/blog/2026/workshop1linux</id><content type="html" xml:base="https://dnakamuraz.github.io/blog/2026/workshop1linux/"><![CDATA[<p>This tutorial is Part 1 of the Museomics Workshop (CVZoo XIV 2025, University of São Paulo, Brazil). Because most programs used to assemble historical DNA sequences in museomics and to perform downstream phylogenetic analyses do not provide a graphical user interface (GUI), the goal of this tutorial is to introduce students to the Linux command line.</p> <p>Before starting the tutorial, download the resources <a href="https://drive.google.com/drive/folders/1FBC4-8E0TquQqtSvxvlzb8qbxzKSuuGg?usp=sharing">here</a>. Unzip the files and go to the output directory (try <code class="language-plaintext highlighter-rouge">cd /mnt/c/Users/Aluno/Downloads/CVZoo*/CVZoo/</code>).</p> <p>Check Part 2 of the Museomics Workshop <a href="https://dnakamuraz.github.io/blog/2026/workshop2assembly/">here</a>.</p> <h2 id="what-is-linux">What is Linux?</h2> <p>Linux was created in 1991 by Linus Torvalds (1969, Finland) as a free and open-source kernel that controls CPU, manages memory, handles files, and communicates with hardware. However, kernels are not operating systems (i.e. Linux is not like Windows and macOS). In parallel with Linux, the GNU Project was initiated by Richard Stallman (1953, USA) in 1983 to include command-line tools, compilers, libraries, and shells. As such, most systems commonly referred to as “Linux” should more properly be called “GNU/Linux”.</p> <p>Linux systems can be distributed in different packages (distros). Some popular distros are:</p> <table> <thead> <tr> <th>Distribution</th> <th>Pros</th> <th>Cons</th> </tr> </thead> <tbody> <tr> <td><strong>Ubuntu</strong></td> <td>Beginner-friendly; large community and documentation; excellent hardware support; widely used in servers, cloud, and bioinformatics</td> <td>Heavier system; less conservative than Debian; some design choices are controversial</td> </tr> <tr> <td><strong>Debian</strong></td> <td>Extremely stable and reliable; conservative updates; large repositories; foundation of many other distros</td> <td>Older software versions; hardware support may lag; less beginner-oriented</td> </tr> <tr> <td><strong>Arch Linux</strong></td> <td>Minimal and highly customizable; rolling release; excellent documentation (Arch Wiki); great for learning Linux internals</td> <td>Steep learning curve; manual installation; higher risk of breakage; not beginner-friendly</td> </tr> <tr> <td><strong>SteamOS</strong></td> <td>Optimized for gaming; excellent performance on Steam Deck; showcases Linux on consumer devices</td> <td>Not designed for general-purpose computing; limited customization; unsuitable for scientific workflows</td> </tr> <tr> <td><strong>Zorin OS</strong></td> <td>Very beginner-friendly; Windows/macOS-like interface; Ubuntu-based with good software support</td> <td>Smaller community; limited flexibility; rarely used in research or servers</td> </tr> <tr> <td><strong>Linux Mint</strong></td> <td>User-friendly and stable; traditional desktop layout; lightweight; based on Ubuntu or Debian</td> <td>Slower adoption of new technologies; desktop-focused; uncommon in servers or HPC</td> </tr> </tbody> </table> <p>When mobile distributions are taken into account, Android is the most popular Linux-based system. As such, most people are Linux users, even if they are unaware of it.</p> <h2 id="using-linux-on-windows">Using Linux on Windows</h2> <p>Windows is the most popular operating system. However, most programs used in bioinformatics are only available for Linux and macOS, because both are UNIX-based (a family of operating systems designed for scientific computing in 1969-1971 at AT&amp;T Bell Labs).</p> <p>To use Linux commands in Windows, there are four options:</p> <ul> <li>Remote Linux: Connecting to a Linux server from a local Windows machine (cons: servers are not available to all users).</li> <li>Dual Boot: Installing Linux and Windows in the same computer (cons: requires storage partitioning).</li> <li>Virtual machine: Emulating Linux inside Windows (cons: slow, requires more RAM and CPU than a native Linux).</li> <li>Windows Subsystem for Linux (WSL): Using a compatibility layer (cons: a few tools may be incompatible).</li> </ul> <p>Given that servers are unavailable, Dual Boot requires splitting the disk, and virtual machines are slow in most cases, the best solution is WSL. Most software is compatible with WSL. The first version of WSL was released in 2016 by Microsoft, but most tools were incompatible. Recently, WSL 2 was released in 2019, with native support for Docker, Conda, and modern Linux tools. Microsoft created WSL because Linux dominates servers, cloud, high-performance, and scientific computing. Using WSL, beginner scientists can continue using Windows but also using Linux tools (although I recommend that all scientists migrate to Linux).</p> <p>To install WSL 2, Windows 10 or 11 are required:</p> <ol> <li>Open PowerShell as Administrator.</li> <li>Run the command <code class="language-plaintext highlighter-rouge">wsl --install</code></li> <li>Restart your computer.</li> <li>Open Ubuntu, create a Linux username and password.</li> <li>Inside Ubuntu, run:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt upgrade
</code></pre></div></div> <p>The Windows files are usually located at <code class="language-plaintext highlighter-rouge">/mnt/c/</code>.</p> <h2 id="basic-commands">Basic commands</h2> <p>To run Linux from the command line, the user must open a <strong>shell</strong>, a program that sends the commands from the user to the operating system. In GNU/Linux, the most common shell is called <strong>Bash</strong> (Bourne Again Shell).</p> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: If the Bash prompt starts with `alan@turing:~\$`, `alan` is the user name, `turing` is the name of the machine (hostname), `~` is the current directory, and `\$` indicates the type of user (`$` = normal user, `#` = root/admin). </div> <p>In Bash, a standard input (<em>stdin</em> e.g. hDNA reads or cladistic matrix) is given to a command, which produces the standard output (<em>stdout</em> e.g. assembled contigs or phylogenetic trees). In addition to standard input and output, programs also produce a standard error (<em>stderr</em>), which is typically used for warnings and error messages.</p> <p>In Bash, the commands are case-sensitive (e.g. <code class="language-plaintext highlighter-rouge">echo</code> is a valid command, whereas <code class="language-plaintext highlighter-rouge">Echo</code> or <code class="language-plaintext highlighter-rouge">ECHO</code> are not). Moreover, pressing the “Tab” key attempts to autocomplete commands and file names (helpful for lazy users). The semicolon <code class="language-plaintext highlighter-rouge">;</code> executes commands sequentially and independently, whereas the pipe <code class="language-plaintext highlighter-rouge">|</code> connects the output of one command directly to the input of the next.</p> <h3 id="navigating-the-filesystem">Navigating the filesystem</h3> <p>When working in Linux, especially in bioinformatics projects, analyses are organized into directories containing raw data, intermediate files, and results. Efficient navigation of the filesystem is therefore essential.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">pwd</span>      <span class="c"># show current directory</span>
<span class="nb">ls</span> <span class="nb">.</span>     <span class="c"># list files in the current directory</span>
<span class="nb">ls</span> <span class="nt">-lh</span>   <span class="c"># list files with sizes</span>
<span class="nb">ls</span> <span class="nt">-a</span>    <span class="c"># list all files (including hidden files)</span>
<span class="nb">ls</span> <span class="nt">-lhS</span>  <span class="c"># list files sorted by size</span>
<span class="nb">ls</span> <span class="nt">-ltr</span>  <span class="c"># list files sorted by modification time</span>
<span class="nb">cd</span> ..    <span class="c"># go up one directory</span>
<span class="nb">cd</span> ../.. <span class="c"># go up two directories</span>
<span class="nb">cd</span> ~     <span class="c"># go to home directory</span>
<span class="nb">cd</span> /     <span class="c"># go to the root directory</span>
<span class="nb">cd</span> -     <span class="c"># return to the previous directory</span>
file     <span class="c"># classify a file</span>
<span class="nb">du</span> <span class="nt">-sh</span>   <span class="c"># size of current directory</span>
</code></pre></div></div> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: The current directory is indicated by a single dot `.`, whereas the parent directory is indicated by two dots `..`. The home directory is indicated by `~` and the root by `/`. </div> <blockquote> <p><strong>Exercise 1</strong></p> <p>In a single line, which command sequentially (1) list files with sizes in the current directory, (2) list all files in the parent directory, (3) go to the parent directory, and (4) show the path to the current directory?</p> <p>A) <code class="language-plaintext highlighter-rouge">ls -lh .; ls -a ..; cd ..; pwd</code></p> <p>B) <code class="language-plaintext highlighter-rouge">ls -lh ..; ls -a .; cd ..; pwd ..</code></p> <p>C) <code class="language-plaintext highlighter-rouge">ls -lh . ls -a .. cd .. pwd</code></p> <p>D) <code class="language-plaintext highlighter-rouge">ls -lh .| ls -a .. | cd .. | pwd</code></p> <details> <summary>See the answer</summary> A </details> </blockquote> <blockquote> <p><strong>Exercise 2</strong> Consider that you are in museomics/part1/ex2:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>museomics/
└── part1/
    ├── ex2/
    │   ├── sample1.fq
    │   ├── sample2.fq
    │   └── consensus.fas
    └── ex3/       
</code></pre></div> </div> <p>Which single-line command prints the current directory, lists all files in the current directory, identifies the file type of sample1.fq, shows the total disk usage of the directory.</p> <p>A) <code class="language-plaintext highlighter-rouge">pwd; ls -a; file sample1.fq; du -sh</code></p> <p>B) <code class="language-plaintext highlighter-rouge">pwd; ls -a; file ../sample1.fq; du -sh data</code></p> <p>C) <code class="language-plaintext highlighter-rouge">pwd | ls | file sample1.fq | du -sh ..</code></p> <p>D) <code class="language-plaintext highlighter-rouge">pwd; ls; file raw/sample1.fastq; du -sh .</code></p> <details> <summary>See the answer</summary> A </details> </blockquote> <h3 id="managing-files-and-using-wildcards">Managing files and using wildcards</h3> <p>Once you can navigate the filesystem, the next essential skill is creating, copying, moving, renaming, and deleting files and directories. These operations are fundamental in bioinformatics, where workflows typically involve organizing raw data, intermediate files, and results into structured directories.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp </span>file <span class="nb">.</span>         <span class="c"># duplicate files to the current directory</span>
<span class="nb">cp</span> <span class="nt">-r</span> directory <span class="nb">.</span> <span class="c"># copy a directory to the current directory</span>
<span class="nb">mkdir </span>new_dir     <span class="c"># make a new directory called new_dir</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> new/dir  <span class="c"># create parent directories if necessary</span>
<span class="nb">mv</span> ../file <span class="nb">.</span>      <span class="c"># move files</span>
<span class="nb">rm </span>file           <span class="c"># remove files</span>
<span class="nb">rm</span> <span class="nt">-r</span> directory   <span class="c"># remove directories</span>
<span class="nb">rm</span> <span class="nt">-ri</span> directory  <span class="c"># remove directory asking for confirmation</span>
</code></pre></div></div> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: Wildcards are used to match groups of files or text efficiently. The asterisk `*` matches any number of characters (including none; e.g. `*.fastq` matches all FASTQ files, and `sample*` matches all files starting with “sample”). The question mark `?` matches exactly one character (e.g. `sample?.fasta` matches sample1.fasta but not sample10.fasta). Square brackets `[]` define character classes, matching one character from a set or range (e.g., `sample[1-3].fastq` matches sample1.fastq, sample2.fastq, and sample3.fastq). Curly braces `{}` enable brace expansion, generating multiple strings rather than matching patterns (e.g., `sample{A,B}.fasta` expands to sampleA.fasta and sampleB.fasta). </div> <blockquote> <p><strong>Exercise 3</strong> You are in museomics/part1/ex3, a directory containing:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>consensus.fas
sample1.fq
sample2.fq
notes.txt
</code></pre></div> </div> <p>Write a single command line that: (1) Creates a directory called <code class="language-plaintext highlighter-rouge">raw</code>, (2) moves all FASTQ (= fq) files into <code class="language-plaintext highlighter-rouge">raw</code>.</p> <details> <summary>See the answer</summary> mkdir raw; mv *.fq raw/ </details> </blockquote> <blockquote> <p><strong>Exercise 4</strong> You are working in the directory museomics/part1/ex4, which contains the following files:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>consensus.fas
sample1_R1.fq
sample1_R2.fq
sample2_R1.fq
sample2_R2.fq
sample3_R1.fq
sample3_R2.fq
control_R1.fq
control_R2.fq
notes.txt
</code></pre></div> </div> <p>In a single command, (1) create the directories analysis/reads and analysis/reads2 if it does not exist, (2) copy the FASTQ files from samples 1 and 3 (both R1 and R2) into analysis/reads, and (3) copy all files R2 into analysis/reads2.</p> <details> <summary>See the answer</summary> mkdir -p analysis/reads; mkdir analysis/reads2; cp sample[13]_R*.fastq analysis/reads/; cp *R2.fq analysis/reads2/ </details> </blockquote> <h3 id="reading-writing-and-selecting-text">Reading, writing, and selecting text</h3> <p>Bioinformatics workflows rely on text files, including FASTA, FASTQ, SAM/BAM (text/binary), VCF, and comma- and tab-delimited tables. Linux provides powerful command-line tools to view these large text files.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat </span>file              <span class="c"># print the file</span>
<span class="nb">echo </span>hello            <span class="c"># print strings</span>
less file             <span class="c"># view files safely</span>
less <span class="nt">-N</span> file          <span class="c"># view files with line numbers</span>
<span class="nb">head </span>file             <span class="c"># print the first 10 lines</span>
<span class="nb">head</span> <span class="nt">-n</span> 20 file       <span class="c"># print the first 20 lines</span>
<span class="nb">paste </span>file1 file2     <span class="c"># merge two files</span>
<span class="nb">paste</span> - -             <span class="c"># merge each two lines from stdin</span>
<span class="nb">tail </span>file             <span class="c"># print the last 10 lines</span>
<span class="nb">tail</span> <span class="nt">-n</span> 20 file       <span class="c"># print the last 20 lines</span>
<span class="nb">wc </span>file               <span class="c"># count lines, words, and characters</span>
<span class="nb">wc</span> <span class="nt">-l</span> file            <span class="c"># count lines</span>
<span class="nb">wc</span> <span class="nt">-w</span> file            <span class="c"># count words</span>
<span class="nb">wc</span> <span class="nt">-c</span> file            <span class="c"># count bytes</span>
<span class="nb">wc</span> <span class="nt">-m</span> file            <span class="c"># count characters</span>
zcat file             <span class="c"># print gzipped files in linux</span>
gzcat file            <span class="c"># print gzipped files in macOS</span>
</code></pre></div></div> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: Avoid using `cat` and `zcat` for large files due to memory limitation. Instead, use `less` or `zcat | head`. </div> <p>In addition to text visualization, Linux provides commands to manipulate files.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">grep</span>                   <span class="c"># search for patterns</span>
<span class="nb">grep</span> <span class="nt">-i</span>                <span class="c"># search for patterns ignoring case</span>
<span class="nb">grep</span> <span class="nt">-v</span>                <span class="c"># inverted search (excluding patterns)</span>
<span class="nb">grep</span> <span class="s2">"ˆ"</span>               <span class="c"># match beginning of line </span>
<span class="nb">grep</span> <span class="s2">"$"</span>               <span class="c"># match end of line</span>
<span class="nb">cut</span>                    <span class="c"># extract specific columns from tables</span>
<span class="nb">cut</span> <span class="nt">-d</span>                 <span class="c"># specify delimiter (default: TAB)</span>
<span class="nb">sed</span> <span class="s1">'s//g'</span>             <span class="c"># substitute 's/pattern/new_pattern/g'</span>
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s1">'s//g'</span>          <span class="c"># substitute within the file (irreversible)</span>
<span class="nb">sed</span> <span class="s1">'1~3 y/actg/ACTG/'</span> <span class="c"># substitute actg with ACTG in lines 1 + every 3 lines (only works in GNU/Linux, not macOS)</span>
<span class="nb">sort</span>                   <span class="c"># sort lines alphabetically or numerically</span>
<span class="nb">sort</span> <span class="nt">-r</span>                <span class="c"># sort using reverse order</span>
<span class="nb">sort</span> <span class="nt">-k</span>                <span class="c"># sort by column</span>
<span class="nb">sort</span> <span class="nt">-t</span>                <span class="c"># specify delimiter</span>
<span class="nb">sort</span> <span class="nt">-u</span>                <span class="c"># unique lines</span>
<span class="nb">tr </span>acgt ACGT           <span class="c"># translate each character in set1 to each character in set2</span>
<span class="nb">uniq</span>                   <span class="c"># deduplicate lines (requires sorted input)</span>
nano                   <span class="c"># beginner-friendly text editor</span>
vi                     <span class="c"># advanced text editor</span>
</code></pre></div></div> <blockquote> <p><strong>Exercise 5</strong> You are working in the directory museomics/part1/ex5, which contains the FASTA file called <code class="language-plaintext highlighter-rouge">genes.fasta</code>. The first three sequences are:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; COI_Homo_sapiens
ATGCTAGCTAGCTAGC
&gt; COI_Mus_musculus
ATGCTAGCTAGCTAGC
&gt; cytb_homo_sapiens
ATGCGGATCGATCGTA
</code></pre></div> </div> <p>There are dozens of sequences, so counting or manipulating them manually is too laborious. Which commands should we use to (1) list only the FASTA headers, (2) count how many sequences are in the file, and (3) count how many sequences contain the word Homo, ignoring case.</p> <details> <summary>Show answer</summary> (1) grep "^&gt;" genes.fasta <br/> (2) grep "^&gt;" genes.fasta | wc -l <br/> (3) grep -i "homo" genes.fasta | wc -l </details> </blockquote> <blockquote> <p><strong>Exercise 6</strong> You are working in the directory museomics/part1/ex6, which contains the FASTQ file called <code class="language-plaintext highlighter-rouge">reads.fq.zip</code>. The first four reads are:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@read003
atGCTAGCtAGC
+
IIIIIIIIIIII
@read002_LOWQUAL
atgctagnnnn
+
!!!!!IIIII!!
@read003
atGCTAGCtAGC
+
IIIIIIIIIIII
@read004_LOWQUAL
ATGCGTAGCTAG
+
!!!!!!!IIIII
</code></pre></div> </div> <p>(1) Remove all reads marked as LOWQUAL (remove the entire read, not only the header), (2) deduplicate sequences, and (3) convert all nucleotides to uppercase.</p> <details> <summary>Show answer</summary> (1) cat reads.fq | paste - - - - | grep -v "LOWQUAL" <br/> (2) cat reads.fq | paste - - - - | sort -u <br/> (3) cat reads.fq | tr actgn ACTGN <br/> Given that solution for the third task also replaces actg in the headers, a better solution (only works in Linux) is: <br/> (3) cat reads.fq | sed '2~4 y/actgn/ACTGN/' </details> </blockquote> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: Each read comprises four lines. To merge the four lines into a single line (so that each line is located in a single line), use `cat reads.fq | paste - - - -` </div> <h2 id="installing-software">Installing software</h2> <p>Installing tools efficiently is key to running analyses reproducibly. This tutorial covers the main ways to install and manage software in Linux. The examples are the programs required in the Part 2 of the Museomics Workshop: gargamel, FASTQC, Cutadapt, Tally, FastqScreen, BWA, MITObim, samtools, bamtools, TNT, and PhyG.</p> <h3 id="binaries">Binaries</h3> <p>Some software comes as a ready-to-use binary, meaning you don’t need to compile it. You only need to change the permissions of the binary and move it to the PATH.</p> <p>Every file and directory in Linux has three types of permissions:</p> <ul> <li>(1) read (r), permission to view the file contents or list a directory;</li> <li>(2) write (w), permission to modify the file or directory;</li> <li>(3) execute (x), permission to run a program or enter a directory.</li> </ul> <p>Every file has permissions specified for three types of users:</p> <ul> <li>Owner (u), the user who owns the file;</li> <li>Group (g), the users in the same group as the file;</li> <li>Others (o), everyone else.</li> </ul> <p>When you type <code class="language-plaintext highlighter-rouge">ls -l</code> in Bash, the first string in each line indicates the permissions. For instance:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> <span class="nb">ls</span> <span class="nt">-l</span>
<span class="nt">-rwxr-xr--</span> 1 alice bioinfo 2345 Jan 18 12:00 analysis.sh
</code></pre></div></div> <p>The user <code class="language-plaintext highlighter-rouge">alice</code> owns the file <code class="language-plaintext highlighter-rouge">analysis.sh</code> of 2345 bytes, last modified on January 18th. The permissions of this file for the owner, group, and others are <code class="language-plaintext highlighter-rouge">rwx</code> (reading, writing and executing), <code class="language-plaintext highlighter-rouge">r-x</code> (only reading and executing), and <code class="language-plaintext highlighter-rouge">r--</code> (only reading), respectively. That is, users in the same group can only read and execute the file, whereas users that are not part of the group can only read it. If Alice wants to make this file executable to all users, she can use the command <code class="language-plaintext highlighter-rouge">chmod +x analysis.sh</code>. However, the program <code class="language-plaintext highlighter-rouge">analysis.sh</code> can only be used if the user is in the same directory where it is located (e.g. typing <code class="language-plaintext highlighter-rouge">./analysis.sh</code>) or if the user specifies the path to it. This is not convenient, so moving the file <code class="language-plaintext highlighter-rouge">analysis.sh</code> to the PATH (a list of directories that Linux searches when you type a command) will allow Linux to easily execute <code class="language-plaintext highlighter-rouge">analysis.sh</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> <span class="nb">echo</span> <span class="nv">$PATH</span> <span class="c"># list PATH directories</span>
/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin
<span class="o">&gt;</span> <span class="nb">mv </span>analysis.sh /usr/local/bin/ <span class="c"># move the program to one of the PATH directories</span>
</code></pre></div></div> <p>TNT is the most popular program in phylogenetic analyses under parsimony and static homology (nucleotides aligned via a similarity function from MSA, assumes homologies a priori, based on phenetics). In contrast, PhyG performs phylogenetic analyses using dynamic homology (nucleotides aligned via tree-alignment, assumes that the best homology schemes are implied from the optimal tree). Both programs can be installed via binaries.</p> <h4 id="tnt">TNT</h4> <ol> <li>Download TNT v. 1.6 for Linux <a href="https://www.lillo.org.ar/phylogeny/tnt/tnt-linux.zip">here</a></li> <li>Unzip the directory, change the mode of the binary, and move it to the PATH: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>unzip tnt-linux.zip
<span class="nb">chmod</span> +x tnt
<span class="nb">echo</span> <span class="nv">$PATH</span>
<span class="nb">mv </span>tnt /usr/local/bin/
</code></pre></div> </div> </li> </ol> <h4 id="phyg">PhyG</h4> <ol> <li>Download PhyG v. 1.3 for Linux <a href="https://github.com/amnh/PhyG/releases/download/v1.3.0/phyg-linux-x86">here</a></li> <li>Change the mode of the binary and move it to the PATH: <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x phyg-linux-x86
<span class="nb">echo</span> <span class="nv">$PATH</span>
<span class="nb">mv </span>phyg-linux-x86 /usr/local/bin/
</code></pre></div> </div> </li> </ol> <h3 id="environments">Environments</h3> <p>In addition to binaries, most programs in bioinformatics are available in Conda, which is an environment manager widely used in scientific computing to install software and manage dependencies in isolated environments. This is useful to avoid dependency conflicts.</p> <p>Two popular Conda distributions are Anaconda (a large Conda distribution of ca. 3 GB containing hundreds of packages for data science) and Miniconda (a cleaner Conda distribution of ca. 70 Mb containing only Python and a few libraries). Bioconda is not a Conda distribution, but a repository hosting most bioinformatics software.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install conda</span>
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh 

<span class="c"># Re-start shell</span>
<span class="nb">source</span> ~/.bashrc 
conda <span class="nt">--version</span>
</code></pre></div></div> <p>Below, we will install all programs necessary for <a href="https://dnakamuraz.github.io/blog/2026/workshop2assembly/">Part 2 of the Museomics Workshop</a>.</p> <h4 id="seqkit">seqkit</h4> <p>seqkit will be installed in the conda environment called <code class="language-plaintext highlighter-rouge">seqkit</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> seqkit bioconda::seqkit
conda activate seqkit
seqkit
conda deactivate
</code></pre></div></div> <h4 id="fastqc">FastQC</h4> <p>TNT and PhyG are not available in Bioconda. In contrast, FastQC v.0.12.1 is available and will be installed in the conda environment called <code class="language-plaintext highlighter-rouge">fastqc</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install possible dependencies of java for fastqc</span>
<span class="nb">sudo </span>apt update
<span class="nb">sudo </span>apt <span class="nb">install</span> <span class="nt">-y</span> libxtst libxrender1 libxi6 libxrandr2 libxinerama1 libxcursor1

<span class="c"># Create an environment</span>
conda create <span class="nt">-n</span> fastqc bioconda::fastqc
conda activate fastqc
fastqc <span class="nt">-h</span>
conda deactivate
</code></pre></div></div> <h4 id="cutadapt">Cutadapt</h4> <p>Cutadapt v.2.6 will be installed in the environment <code class="language-plaintext highlighter-rouge">cutadapt</code> with Python v.3.7.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> cutadapt <span class="nv">python</span><span class="o">=</span>3.7 bioconda::cutadapt<span class="o">=</span>2.6
conda activate cutadapt
cutadapt <span class="nt">-h</span>
conda deactivate cutadapt
</code></pre></div></div> <h4 id="tally">Tally</h4> <p>Tally (from Reaper v.16.098) will be installed in the environment <code class="language-plaintext highlighter-rouge">tally</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> tally bioconda::reaper
conda activate tally
tally <span class="nt">-h</span>
conda deactivate
</code></pre></div></div> <h4 id="fastqscreen">FastqScreen</h4> <p>FastqScreen v.0.15.3 will be installed in the environment <code class="language-plaintext highlighter-rouge">fastqscreen</code>. In my experience, recent versions can perform poorly in Linux, so use this specific version.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> fastqscreen bioconda::fastq-screen<span class="o">=</span>0.15.3
conda activate fastqscreen
fastq_screen <span class="nt">-h</span>
conda deactivate
</code></pre></div></div> <p>Note that the environment is called <code class="language-plaintext highlighter-rouge">fastqscreen</code>, whereas the command is <code class="language-plaintext highlighter-rouge">fastq_screen</code>.</p> <h4 id="bwa">BWA</h4> <p>BWA v.0.7.17 will be installed in the environment <code class="language-plaintext highlighter-rouge">bwa</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> bwa bioconda::bwa
conda activate bwa
bwa
conda deactivate
</code></pre></div></div> <h4 id="mitobim">MITObim</h4> <p>MITObim v.1.9.1. will be installed in the environment <code class="language-plaintext highlighter-rouge">mitobim</code>. In addition to MITObim, Mira, and MiraConvert will be automatically installed. For macOS and Windows, use Docker (instructions <a href="https://github.com/chrishah/MITObim">here</a>; conda does not work, in my experience). For Linux (including Windows WSL), the following commands are currently working:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> mitobim bioconda::mitobim
conda activate mitobim
MITObim.pl <span class="nt">-h</span>
conda deactivate
</code></pre></div></div> <h4 id="samtools-and-bamtools">Samtools and Bamtools</h4> <p>Samtools and bamtools will be installed in the environment <code class="language-plaintext highlighter-rouge">samtools</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda create <span class="nt">-n</span> samtools <span class="nt">-c</span> conda-forge <span class="nt">-c</span> bioconda <span class="nv">samtools</span><span class="o">=</span>1.23
conda activate samtools
conda <span class="nb">install </span>bioconda::bamtools
samtools
bamtools
conda deactivate
</code></pre></div></div> <div style="border:2px solid rgba(76, 117, 175, 0.87); padding:12px; margin-bottom: 16px; border-radius:8px; background:#E7F0FE"> Tip: If you want to delete an environment, type `conda remove --name NAME --all`. If you are inside an environment and want to uninstall a specific package, type `conda remove NAME`. If you want to list all available environments, type `conda env list`. </div> <h3 id="other-methods">Other methods</h3> <table> <thead> <tr> <th>Method</th> <th>When to use</th> <th>Pros</th> <th>Cons</th> </tr> </thead> <tbody> <tr> <td>Precompiled binaries</td> <td>Single tool, simple</td> <td>Fast, no compilation</td> <td>Manual updates, dependency issues</td> </tr> <tr> <td>Conda / Bioconda</td> <td>Multiple tools, environments</td> <td>Dependency management, reproducible</td> <td>Disk space, environment conflicts</td> </tr> <tr> <td>Compilation</td> <td>Latest version, HPC optimization</td> <td>System optimized</td> <td>Error-prone, requires build tools</td> </tr> <tr> <td>Docker</td> <td>Reproducibility, pipelines</td> <td>Isolated, cross-platform</td> <td>Requires Docker, may be slow</td> </tr> <tr> <td>Modules</td> <td>HPC / cluster</td> <td>Easy switching, no installation</td> <td>Only on supported systems</td> </tr> </tbody> </table> <p>Check the Part 2 of the Museomics Workshop <a href="https://dnakamuraz.github.io/blog/2026/workshop2assembly/">here</a>.</p>]]></content><author><name>Daniel Y. M. Nakamura</name></author><category term="workshop"/><category term="museomics"/><category term="tutorial"/><summary type="html"><![CDATA[Part 1 of the Museomics Workshop]]></summary></entry><entry><title type="html">First release of prepDyn - Python scripts for preprocessing dynamig characters</title><link href="https://dnakamuraz.github.io/blog/2025/prepdyn/" rel="alternate" type="text/html" title="First release of prepDyn - Python scripts for preprocessing dynamig characters"/><published>2025-12-13T14:57:00+00:00</published><updated>2025-12-13T14:57:00+00:00</updated><id>https://dnakamuraz.github.io/blog/2025/prepdyn</id><content type="html" xml:base="https://dnakamuraz.github.io/blog/2025/prepdyn/"><![CDATA[]]></content><author><name></name></author><category term="phylogenetics"/><summary type="html"><![CDATA[Scripts designed to automatically fetch sequences from GenBank, delete trivial nucleotides, identify missing data, and partition]]></summary></entry></feed>